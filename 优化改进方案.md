# 学习助手优化改进方案

> 目标：解决两个核心问题
> 1. **首页用户切换不灵活**，顶部“新增”按钮与设置功能重复；
> 2. **学习统计、图表、报告仍停留在最早初始版本**，数据分散、入口失效、与当前项目脱节，需要重新设计。

---

## 目录

- [一、问题一：首页用户切换优化](#一问题一首页用户切换优化)
  - [1.1 现状分析](#11-现状分析)
  - [1.2 问题清单](#12-问题清单)
  - [1.3 优化方案](#13-优化方案)
  - [1.4 涉及文件](#14-涉及文件)
- [二、问题二：学习统计 / 图表 / 报告重新设计](#二问题二学习统计--图表--报告重新设计)
  - [2.1 现状分析](#21-现状分析)
  - [2.2 问题清单](#22-问题清单)
  - [2.3 设计目标](#23-设计目标)
  - [2.4 总体架构](#24-总体架构)
  - [2.5 分模块设计](#25-分模块设计)
    - [A. 统一统计数据底座](#a-统一统计数据底座)
    - [B. 统一图表层](#b-统一图表层)
    - [C. 统一“学习数据”中心视图](#c-统一学习数据中心视图)
    - [D. 报告模块重构](#d-报告模块重构)
    - [E. 首页仪表盘数据打通](#e-首页仪表盘数据打通)
  - [2.6 涉及文件](#26-涉及文件)
- [三、实施优先级与阶段划分](#三实施优先级与阶段划分)
- [四、风险与注意事项](#四风险与注意事项)

---

## 一、问题一：首页用户切换优化

### 1.1 现状分析

首页（新布局 `useNewLayout`）顶部栏布局如下（见 `Forms/MainForm.cs` 的 `PanelTopBar_Resize`，约 L400-L412）：

```
[logo][标题]                     [🌙 主题切换][➕ 新增用户][用户下拉框 comboBoxNewLayoutUser]
```

- **用户切换**：使用 `comboBoxNewLayoutUser`（`ComboBoxStyle.DropDownList` 下拉框），切换事件 `ComboBoxNewLayoutUser_SelectedIndexChanged`（L1211-L1222）会同步旧下拉框、刷新 Dashboard 欢迎语并触发 `UserChanged`。
- **新增按钮**：`buttonNewUser`（➕，`ButtonNewUser_Click`，L1224）触发 `NewUserClicked` → `MainPresenter.CreateNewUser()`。
- **遗留旧代码**：老布局的 `comboBoxUser` + `groupBoxUser`（L784-L786、L956、L1206）仍与新版下拉框双向同步，属于死代码，增加了维护与误同步风险。

### 1.2 问题清单

| 编号 | 问题 | 影响 |
| --- | --- | --- |
| 1-1 | 顶部用户切换使用 ComboBox 下拉，交互呆板 | 无法直观展示头像、等级、备注；切换需展开列表；与整体卡片化风格不统一 |
| 1-2 | 顶部 ➕ 新增用户按钮与设置重复 | `SettingForm` 的“用户管理”分组（`groupBoxUserManagement`：`listBoxUsers` + `buttonAddUser` + `buttonDeleteUser`）已完整支持新增/删除，首页 ➕ 功能重复 |
| 1-3 | 旧布局 `comboBoxUser`/`groupBoxUser` 遗留 | 死代码，两套下拉同步易出 BUG |

### 1.3 优化方案

**方案：头像 + 昵称的可点击入口 + 弹出式用户切换面板（替代下拉框）**

1. **入口形态**：将顶部“用户下拉框”替换为一个“用户入口”控件：
   - 左侧圆形头像（按用户生成默认底色 + 首字），右侧显示昵称（可含小三角指示符）。
   - 整体作为单个按钮，`Cursor.Hand`，hover 有浅色背景反馈，与主题联动（`ApplyTheme`）。
   - 点击后弹出**浮层面板**（Pop-up Panel / `ContextMenuStrip` 均可，推荐自定义浮层以支持自定义样式与统计预览）。

2. **浮层面板内容**（自上而下）：
   - 当前用户信息区：头像、昵称、等级徽章、连续学习天数等一行概览。
   - 用户列表区：每个用户一行（头像 + 昵称 + 简短统计，如“连续 N 天”），高亮当前用户，点击即切换（复用现有 `SelectedUser` 设置逻辑与 `UserChanged` 事件）。
   - 底部操作区：两个入口项 —— **“➕ 添加用户”** 与 **“⚙️ 管理用户”**，点击后统一跳转到设置窗体的用户管理分组（`OpenSettingsClicked`），**不在首页内联新增**，消除重复。

3. **行为细节**：
   - 切换后关闭浮层，刷新首页欢迎语与统计（现有 `dashboardView.UserName`、`UserChanged` 链路保持）。
   - 支持键盘：焦点在入口上按 `Enter/Space` 打开、`Esc` 关闭、`↑/↓` 选择用户。
   - 用户列表数据复用 `RefreshUserList` / `MainPresenter.RefreshUsers`；设置窗体关闭后刷新（MainForm L591-L592 已有此逻辑，保持）。

4. **删除**：
   - 删除顶部 ➕ `buttonNewUser`（含 `ButtonNewUser_Click`、`NewUserClicked` 事件订阅链 `View_NewUserClicked` → `CreateNewUser` 可一并移除，或保留事件但入口只在设置中）。
   - 清理旧布局 `comboBoxUser`、`groupBoxUser`、`labelUser` 及其同步逻辑（`ComboBoxUser_SelectedIndexChanged`、`RefreshNewLayoutUserList` 中同步旧框的代码）。
   - 移除 `buttonNewUser` 相关主题设置（MainForm L99-L102）与顶部布局代码（L1141-L1154）。

5. **可访问性/扩展**：入口控件独立成 `UserSwitcherControl`（UserControl），便于后续接入“用户对比”“最近用户”等扩展。

### 1.4 涉及文件

| 文件 | 改动 |
| --- | --- |
| `Forms/MainForm.cs` | 新增/替换顶部用户入口控件；删除 `buttonNewUser`、旧 `comboBoxUser`/`groupBoxUser` 及同步逻辑；调整 `PanelTopBar_Resize` |
| `Forms/UserControls/Navigation/`（新增 `UserSwitcherControl.cs`） | 用户入口 + 浮层面板控件 |
| `Presenters/MainPresenter.cs` | 评估 `CreateNewUser`/`NewUserClicked` 订阅是否保留（建议移除首页入口） |
| `Forms/SettingForm.cs` | 保持现状（作为新增/删除用户的唯一入口），可补充“从首页跳转定位到用户管理分组”的支持 |

---

## 二、问题二：学习统计 / 图表 / 报告重新设计

### 2.1 现状分析

当前“统计、图表、报告”是早期初始版本，存在**数据分散、统计口径不统一、UI 多套并存、图表实现重复、入口失效、关键数据未记录**六大问题。

**（1）数据层：多套来源、口径不一**

| 数据 | 存储位置 | 说明 |
| --- | --- | --- |
| 学习活动计数 | `LearningAnalyticsService`（JSON 每用户文件 `AppPaths.GetUserAnalyticsPath`，`UserAnalyticsData.DailyRecords/CategoryStats`） | 仅累加 `ItemsLearned/ItemsReviewed/CorrectCount/WrongCount`；`RecordActivity` 不写学习时长 |
| 经验/等级/连击 | `StudyStatsManager`（DB `StudyStats`，`XP=次数*100`） | 被 `GamificationService` 使用 |
| 今日学习/连续天数/学习进度 | `UserProfile`（DB） | 供首页 Dashboard 与状态栏 |
| 间隔重复 | DB `SpacedRepetitionItems` + `ReviewLogs` | 已有保留率、遗忘曲线、复习效率、热力图能力，数据较完整 |
| 错题 | DB `WrongAnswerItem`（`IWrongAnswerService`） | 独立 |
| 目标 | `ILearningGoalService` | 独立 |
| 番茄钟 | `PomodoroService`/`PomodoroSession` | 独立，含真实时长 |
| 笔记 / 收藏 | `INoteService` / `IFavoriteService` | 独立 |

> **关键缺陷**：`TimeSpentMinutes` 在整个代码库中**没有任何赋值点**（已全库检索确认），因此“学习时长”相关统计恒为 0，报告中的时长指标全部失真。

**（2）UI 层：多套统计界面并存、语义混乱**

| 界面 | 位置 | 定位 |
| --- | --- | --- |
| `LearningStatsForm` | `Forms/LearningHubForm.cs` 内嵌 | 最早的初版，MiniLineChart，JSON 统计 |
| `LearningManagementForm` | `Forms/LearningManagementForm.cs` | 功能较全：ScottPlot 趋势/遗忘曲线/分类/评分分布图、热力图、目标日历、错题统计、算法切换 —— **当前实际承载统计的主力** |
| `ResultForm` 的“学习统计图表” | `Forms/ResultForm.cs`（`groupBoxChart` + `ChartControl`） | 由 `WindowManager.OpenStatisticsWindow`（L263）→ `ResultPresenter`/`IResultView` 打开，与“统计”语义不符（结果页充当统计页） |
| 首页 StatCard | `Forms/UserControls/Dashboard/DashboardView.cs` | 构造时**写死初始值**（25分钟/7天/120/Lv.5/0/0），仅部分被 `UpdateDashboardStats` 覆盖 |

**（3）统计入口失效**：`MainForm` 侧边栏已把“统计”项注释掉（L212）；`buttonOpenStatistics`（L792）**未挂接 Click 事件**，`PerformClick()`（L440/L537）为空操作。当前侧边栏仅 6 项（首页/学习/内容编辑/PDF阅读/浏览器/设置），**没有统计入口**。

**（4）图表实现重复**：`LearningChartService`（`Services/Learning/LearningChartService.cs`，Bitmap 手绘，旧）与 `Forms/UserControls/Charts/LearningCharts.cs`（ScottPlot，新）并存，风格、主题均不统一。

**（5）报告为纯文本**：`LearningReportService`（`Services/Learning/LearningReportService.cs`）产出 `DailyReport/WeeklyReport/MonthlyReport`，仅生成字符串文本；无图形化报告界面、无统一导出（现有导出仅 `LearningDataExportService` 的 CSV/Excel 复习日志、`WrongAnswerService.ExportToMarkdown` 错题）。

### 2.2 问题清单

| 编号 | 问题 | 影响 |
| --- | --- | --- |
| 2-1 | 统计数据多源、口径不一 | 同一指标在不同界面数值不一致 |
| 2-2 | `TimeSpentMinutes` 从未写入 | 学习时长、时长类报告全部失真 |
| 2-3 | 统计界面多套并存、语义混乱 | 维护成本高，`ResultForm` 充当统计页名不符实 |
| 2-4 | 统计入口失效（侧边栏注释、按钮无事件） | 用户无法从首页进入统计 |
| 2-5 | 图表实现重复（Bitmap 旧版 vs ScottPlot） | 风格/主题不统一，代码冗余 |
| 2-6 | 首页 StatCard 写死初始值 | 空数据展示“假数据”，体验差 |
| 2-7 | 报告纯文本、无导出 | 无法沉淀/分享学习成果 |

### 2.3 设计目标

1. **单一数据真相**：统计以 **DB 为唯一权威数据源**，通过一个聚合服务统一计算，消除多源；历史 JSON 数据一次性迁移。
2. **真实数据**：补全学习时长记录，口径统一且可配置（学习会话 + 番茄钟，可勾选计入 PDF/游戏等），让时长、效率类指标可用。
3. **单一统计中心**：以 `LearningManagementForm` 为基础重构为统一的“学习数据”中心（Tab 视图），废弃 `LearningStatsForm` 与 `ResultForm` 统计角色。
4. **统一图表层**：全面采用 ScottPlot（`LearningCharts.cs`），移除 Bitmap 旧图表服务，支持主题联动。
5. **报告可视化 + 可导出**：报告升级为结构化卡片 + 图表 + 文本，支持 Markdown/TXT/HTML/PDF/Excel 导出，并可由 AI 生成自然语言总结。
6. **入口打通**：侧边栏恢复“统计”入口；首页 StatCard 接入真实数据并链接到统计中心。

### 2.4 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        UI 展示层                              │
│  首页 Dashboard(真实数据)   学习数据中心(LearningManagementForm)│
│  侧边栏“统计”入口            报告卡片(图形化+可导出)             │
└───────────────────────────┬─────────────────────────────────┘
                            │ 读取 DTO
┌───────────────────────────▼─────────────────────────────────┐
│            统一统计聚合服务 (LearningStatsAggregator)          │
│  日/周/月汇总 · 趋势 · 分类 · 记忆(保留率/遗忘曲线/热力图)      │
│  错题 · 目标达成 · 效率(时长/正确率/连续) · 报告 DTO            │
└───────────────────────────┬─────────────────────────────────┘
                            │ 只读聚合
┌───────────────────────────▼─────────────────────────────────┐
│            数据层（DB 唯一权威 + 一次性迁移）                   │
│  StudyStats · ReviewLogs · SpacedRepetitionItems ·           │
│  WrongAnswerItem · PomodoroSession · UserProfile ·           │
│  GoalRecords · Notes · 历史 JSON(迁移后废弃)                  │
└─────────────────────────────────────────────────────────────┘
```

### 2.5 分模块设计

#### A. 统一统计数据底座

**A1. 补齐数据写入**
- 在 `LearningAnalyticsService.RecordActivity` 或统一聚合层增加**学习时长累计**：学习会话（`LearningSession`/`StudyEngine`）结束时按实际耗时写入当日 `TimeSpentMinutes`；同时合并番茄钟 `PomodoroSession` 的完成时长。
- 建议保留 `RecordActivity` 的调用点（`StudyEngine.cs` 内多处），但在其内部改为**同时写 DB**（新表或并入现有表），从根上消除 JSON 持久化。
- **时长口径（可配置）**：除学习会话、番茄钟外，PDF 阅读、游戏化学习、闪卡复习是否计入“学习时长”应统一口径，并在设置中提供开关（默认：学习会话 + 番茄钟计入；PDF/游戏按“学习行为”单列或可勾选计入），避免三个来源的时长对不上。

**A2. 建立统一聚合服务 `LearningStatsAggregator`（新增）**
- 单一入口，聚合以下服务的数据：`StudyStatsManager`（经验/等级/连击）、`ReviewLogs + SpacedRepetitionItems`（记忆）、`WrongAnswerItem`（错题）、`PomodoroService`（时长）、`ILearningGoalService`（目标）、`INoteService`/`IFavoriteService`（内容资产）、`UserProfile`（学习进度）。
- 输出统一 DTO：`DailyOverview`、`WeeklyOverview`、`MonthlyOverview`、`TrendSeries`、`CategoryBreakdown`、`MemoryInsights`、`WrongAnswerSummary`、`GoalProgress`、`EfficiencyReport`。
- **聚合结果缓存**：复用现有 `CacheService`/`CacheManagerService` 缓存聚合结果，并订阅 `EventBus` 学习事件（`LearningEvents`）作为失效触发器，避免每次打开统计中心全量查 DB（学习结束后自动失效刷新）。
- **渐进改造**：保留 `ILearningAnalyticsService` 现有接口签名，内部改由聚合/DB 提供数据，UI 分两批替换为聚合 DTO，避免一次性大重构（该接口被 `StudyEngine`、`LearningForm` 等多处引用）。

**A3. 历史数据迁移**
- 启动迁移任务：把每用户 JSON `UserAnalyticsData`（`AppPaths.GetUserAnalyticsPath`）的 `DailyRecords`/`CategoryStats` 导入 DB；成功后打迁移标记并废弃 JSON 读写（参考 `StudyStatsManager.MigrateFromJsonToDb` 的标记模式）。
- **幂等与可重跑**：迁移以“迁移标记 + 幂等 upsert（按 用户+日期 主键）”实现，中断后可重跑不产生重复数据。

**A4. 统一统计口径**
- 明确定义：学习时长（分钟）= 学习会话 + 番茄钟完成时长；正确率 = Correct/(Correct+Wrong)；连击 = 有学习行为的连续天数；等级/经验以 `GamificationService` 为准。文档化口径并作为聚合层唯一实现，供所有界面复用。
- 口径在设置中提供开关（如是否计入 PDF/游戏时长），聚合层按配置计算。

**A5. DB 索引**
- 迁移/上线时对高频查询字段补索引：`ReviewLogs(UserId, ReviewTime)`、`WrongAnswerItem(UserId, ...)`、`PomodoroSession(UserId, StartTime)` 等，避免数据量增长后统计中心变慢。

#### B. 统一图表层

- **保留**：`Forms/UserControls/Charts/LearningCharts.cs`（ScottPlot）：`LearningTrendChart`、`ForgettingCurveChart`、`CategoryProgressChart`、`ReviewDistributionChart`。
- **新增**：`WeeklyHeatmapChart`、`GoalProgressChart`（或复用现有 `GoalCalendarView`）补齐热力图/目标可视化；新增 `MemoryMaturityChart`（基于 FSRS `Stability/Difficulty` 的“新生/学习中/已掌握”成熟度分布），比单一保留率更直观。
- **删除**：`Services/Learning/LearningChartService.cs`（Bitmap 手绘版）及其接口 `ILearningChartService`，图表统一走 ScottPlot。
- 所有图表控件实现 `IThemeable`，跟随明暗主题；统一配色（沿用现有 `_chartColors` 或主题色板）。

#### C. 统一“学习数据”中心视图

- 以 `LearningManagementForm` 为基座重构为 Tab 布局（`TabControl`）：

| Tab | 内容 |
| --- | --- |
| 概览 | StatCard 组（今日时长/数量/正确率/连续/等级/目标达成）+ 趋势图（`LearningTrendChart`） |
| 记忆 | 遗忘曲线（`ForgettingCurveChart`）+ 保留率 + 记忆成熟度（`MemoryMaturityChart`）+ 复习效率（评分分布 `ReviewDistributionChart`）+ 学习热力图 |
| 错题 | 复用 `WrongAnswerStatsPanel`/`WrongAnswerCard`，支持导出 Markdown |
| 目标 | `GoalCalendarView` + 目标达成进度图 |
| 报告 | 图形化报告卡片（见 D） |
| 对比 | 多用户数据对比（复用 `MainPresenter.LoadUserComparison` 遗留逻辑，支持切换/新增对比用户） |

- **废弃**：`LearningHubForm` 内嵌的 `LearningStatsForm`；`ResultForm` 恢复为纯“学习结果”用途（从 `WindowManager.OpenStatisticsWindow` 解耦）。

**交互增强**
- **实时刷新**：订阅 `EventBus` 学习事件，学习/复习/番茄完成后自动刷新当前统计 Tab 与首页 Dashboard，避免返回时数据仍是旧的（配合 A2 缓存失效）。
- **卡片下钻**：概览 StatCard 可点击进入对应 Tab（今日时长→趋势、正确率→错题、保留率→记忆），而非只看数字。
- **时间范围与筛选**：提供“今天/本周/本月/自定义”范围切换 + 科目/分类筛选，趋势、分类、报告联动。
- **空状态引导**：新用户无数据时显示引导（去学习 / 导入内容），而非满屏 0。
- **灰度切换**：统计中心重构期间保留旧入口，通过功能开关（设置项）平滑切换，便于回滚。

#### D. 报告模块重构

- **保留** `LearningReportService` 的日/周/月计算逻辑，但升级输出：从纯文本 `GenerateReportText` 扩展为结构化报告模型（指标卡 + 趋势图 + 分类分布 + 建议列表），由“报告”Tab 渲染为卡片式页面。
- 新增**导出**能力：复用/扩展 `LearningDataExportService`，支持报告导出为 Markdown / HTML / TXT；同时复用现有 `ExportService` 的 Excel 能力补 **PDF/Excel** 导出（HTML 内嵌 ScottPlot 生成的图片或 SVG 简单图表）。
- **AI 报告（增值）**：复用已集成的 Deepseek/Doubao AI 服务（`DeepseekAIService`/`DoubaoAIService`），由 AI 生成周报/月报的自然语言总结与个性化建议，替代纯规则文案；无网络或调用失败时回退到现有规则文案（不阻塞）。
- **日报自动提醒**：复用 `ILearningReminderService`，每日定时推送“昨日学习小结”（时长/数量/正确率/连击），让统计被动触达用户。
- 建议由聚合层统一喂数，`LearningReportService` 不再直接依赖 `ILearningAnalyticsService`（改依赖聚合 DTO）。

#### E. 首页仪表盘数据打通

- 移除 `DashboardView` 构造时的写死初始值（L122-L133），改为统一聚合服务返回**真实数据**，无数据显示占位（“暂无数据，开始学习吧”）而非假数值。
- `UpdateDashboardStats`（`MainPresenter`）改由 `LearningStatsAggregator` 汇总（时长/连击/经验/等级/挑战/笔记）后下发。
- 侧边栏**恢复“统计”入口**：新增 `NavigationItem { Key="statistics", Text="📊 学习数据" }`，点击打开统一学习数据中心；修复/移除 `buttonOpenStatistics` 死路径，统一改走 `WindowManager.OpenLearningManagementWindow`（或新方法 `OpenStatisticsCenter`）。

### 2.6 涉及文件

| 类型 | 文件 | 改动 |
| --- | --- | --- |
| 新增 | `Services/Learning/LearningStatsAggregator.cs` + 接口 | 统一聚合服务（含聚合结果缓存与 EventBus 失效订阅） |
| 新增 | `Forms/UserControls/Charts/WeeklyHeatmapChart.cs`、`GoalProgressChart.cs`、`MemoryMaturityChart.cs` | 补齐图表 |
| 新增 | `Services/AI/LearningReportAIService.cs` | AI 报告总结（失败回退规则文案） |
| 修改 | `Services/Learning/LearningAnalyticsService.cs` | 补时长写入、JSON→DB、只做数据源 |
| 修改 | `Services/Learning/StudyEngine.cs` | 学习会话结束时记录真实时长 |
| 修改 | `Common/Events/LearningEvents.cs` | 按需新增统计失效/刷新事件 |
| 修改 | `Common/ServiceCollectionExtensions.cs` | 注册聚合服务、AI 报告、图表；移除旧图表注册 |
| 修改 | `Services/Learning/LearningReportService.cs` | 结构化报告模型 + 依赖聚合层 |
| 修改 | `Services/Learning/LearningDataExportService.cs` | 报告导出（MD/HTML/TXT + PDF/Excel） |
| 修改 | `Forms/LearningManagementForm.cs` | Tab 化重构为学习数据中心（含交互增强） |
| 修改 | `Forms/SettingForm.cs` | 新增时长口径开关、统计中心灰度开关 |
| 修改 | `Forms/LearningHubForm.cs` | 移除 `LearningStatsForm` |
| 修改 | `Forms/ResultForm.cs`、`Managers/WindowManager.cs` | 统计与结果页解耦 |
| 修改 | `Forms/MainForm.cs`、`Forms/UserControls/Dashboard/DashboardView.cs` | 侧边栏统计入口、首页真实数据、空状态 |
| 修改 | `Presenters/MainPresenter.cs` | Dashboard 数据来自聚合服务；启用多用户对比页 |
| 删除 | `Services/Learning/LearningChartService.cs`（含接口） | 旧 Bitmap 图表 |

---

## 三、实施优先级与阶段划分

### 阶段一：P0（打通数据底座，让统计“真实”）
1. `TimeSpentMinutes` 写入（学习会话 + 番茄钟），时长口径可配置（设置开关）。
2. 新增 `LearningStatsAggregator`，输出统一 DTO，作为唯一统计口径；**聚合结果缓存 + EventBus 失效**。
3. JSON → DB 一次性迁移（幂等、可重跑），并补高频查询索引。
4. 首页 StatCard 接入真实数据、移除写死初始值、增加空状态。
5. 补**聚合口径测试**（`LearningAssistant.Tests`：时长=会话+番茄、正确率、连击），作为后续重构护栏。

### 阶段二：P1（收敛 UI，统一入口与图表）
6. 恢复侧边栏“统计”入口，修复 `buttonOpenStatistics` 死路径，统一走学习数据中心。
7. `LearningManagementForm` Tab 化重构；废弃 `LearningStatsForm`；`ResultForm` 与统计解耦；接入**实时刷新、时间范围/科目筛选、卡片下钻、空状态引导**。
8. 统一图表层：全面用 ScottPlot，删除 Bitmap `LearningChartService`；补齐热力图/目标图/记忆成熟度图；图表主题联动。
9. 统计中心加**灰度开关**（设置项），旧入口并存便于回滚。

### 阶段三：P2（报告升级 + 增值能力 + 首页用户切换）
10. 报告结构化 + 图形化渲染 + 导出（MD/HTML/TXT + PDF/Excel）。
11. AI 周报/月报总结（失败回退规则文案）；日报自动提醒（`ILearningReminderService`）。
12. 统计中心“对比”Tab 启用多用户数据对比（复用遗留逻辑）。
13. 首页用户切换改版（浮层面板 + 移除 ➕ 按钮 + 清理旧布局死代码）。

> 阶段一/二不互相阻塞，可并行；阶段三可与阶段一并行。用户切换优化（问题一）独立，可随时插入任意阶段实施。

---

## 四、风险与注意事项

1. **数据迁移安全**：JSON→DB 迁移需先备份 `AppData/`；采用“迁移标记 + 幂等 upsert”避免重复迁移；迁移失败不影响启动（降级为不迁移并打日志）。
2. **聚合服务回归风险**：涉及 `StudyEngine`（学习记录链路）、`GamificationService`（经验/等级）、`MainPresenter`（首页）多处调用，需跑 `LearningAssistant.Tests`（现有 `LearningAnalyticsServiceTests`、`StudyEngineTests`、`UserSessionServiceTests` 等）保障口径一致。
3. **`RecordActivity` 高频调用**：若改为同步写 DB，注意性能，建议聚合/异步写入或批量落库，避免阻塞学习主流程。
4. **迁移与写入并发**：迁移期间 `RecordActivity` 可能仍在写 JSON/DB，迁移需处理并发（迁移先于日常写入或加锁），避免丢数据。
5. **缓存失效遗漏**：若某些学习路径未触发 `EventBus` 事件，聚合缓存会展示旧数据；失效触发器需覆盖全部写入路径，并保留“打开统计中心强制刷新”兜底。
6. **移除旧组件的影响面**：删除 `LearningChartService`、`LearningStatsForm`、`buttonOpenStatistics` 前先全局检索引用（已有 `ServiceCollectionExtensions.cs` L235 注册、`WindowManager` L263、`LearningHubForm` 等），避免残留编译错误。
7. **AI 报告成本与可用性**：AI 调用需限频、缓存结果，无网络/失败时回退规则文案，保证报告不因 AI 不可用而中断。
8. **行为兼容与回滚**：用户切换改造后需保持 `UserChanged`/`SelectedUser` 对外接口与设置窗体刷新逻辑不变；统计中心重构期间通过灰度开关保留旧入口，便于回滚。
