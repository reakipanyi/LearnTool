# 记忆翻牌 设计方案

> 项目：LearningAssistant（WinForms + WebView2）
> 目标：在现有"单词消消乐"基础上，新增一款 **"记忆翻牌"（Memory / Concentration）** 游戏，卡片初始背面朝上，玩家凭记忆翻牌配对单词与释义。
> 本文档只做方案设计，不涉及代码实现。

---

## 1. 背景与目标

"单词消消乐"（配对消除）已打通 WebView2 静态页 + 内容编辑器数据 + 成绩回写闭环。但配对消除的卡片**全部正面朝上**，玩家是"找"答案；缺少一款**考验记忆、先翻开后凭记忆配对**的玩法，以锻炼"看到释义→回忆单词"的主动回忆能力。

本方案设计 **"记忆翻牌"**：
- 复用消消乐已验证的整套技术路径（WebView2 静态页 + `ContentLoaderService` + 成绩回写）；
- 玩法核心差异：**卡片初始背面朝上，翻两张配对，配对失败翻回**，考验短期记忆；
- 纯静态网页（HTML + CSS + JS），无第三方框架。

---

## 2. 玩法设计

### 2.1 核心规则（记忆翻牌 / Memory Match）
在一张网格盘面上放置 2N 张卡片（N 对 = N 张"单词卡" + N 张"释义卡"），**全部背面朝上**。玩家一次翻开两张：
- **配对成功**（一张单词 + 一张释义，属于同一词条）：两张卡片保持正面并加特效，随后可消除或置灰；
- **配对失败**：两张卡片高亮/抖动后 **自动翻回背面**，记一次尝试错误；
- 全部 N 对配对完成 → 通关结算。

> 与"单词消消乐"的本质区别：消消乐卡片正面朝上直接可见，玩家靠**找**；记忆翻牌卡片初始隐藏，玩家必须**记住**翻过的卡片位置与内容，才能高效配对。

### 2.2 配牌与难度
- 每局抽取 N 个词条（推荐 **N=8**，即 16 张卡，4×4 或 4 列自适应），兼顾记忆难度与盘面布局。
- **抽词策略（错题优先）**：优先选入**错题本/低掌握度**词条（基于 `LearningItem.Status`、`ReviewCount`、错题记录），让游戏成为复习薄弱词的入口；答对后降权/移出错题，答错再加重。默认仍可随机抽取。
- 难度分级（可做前端开关）：
  - **简单**：N=6，配对后卡片清除，不加时/不限步数；
  - **普通**：N=8，配对后卡片置灰保留，限时；
  - **困难**：N=10，显示词条但隐藏音标，限时 + 错误次数上限。
- **难度动态化（可选）**：按实时答对率/错误次数自适应调整 N 或限时，避免固定难度带来的挫败或无聊。

### 2.3 计分与反馈
- 计分：基础分 + **连击加成**（连续配对成功加分） − 错误翻牌惩罚。
- **星级/评价**：按"步数 + 用时 + 错误次数"综合评定（参考结算页的 🏆/🎉/😊/💪）。
- **同类快速提示**：翻第一张是单词、第二张也是单词 → 立即提示"请翻一张释义卡"并退回，**不消耗步数、不计错误**，避免玩家在同类卡片间反复浪费。
- **记忆辅助（可选）**：提供"提示"按钮，消耗 1 次机会高亮一张尚未翻开的目标卡，降低挫败感，适合儿童。
- 反馈：
  - 翻牌：3D 翻转动画（CSS `rotateY`）；
  - 配对成功：卡片高亮 → 粒子/缩放特效 → 消除或置灰，播正确音效；
  - 配对失败：两张卡片抖动 → 1 秒后翻回背面，播提示音；
  - 发音：点击已翻开的单词卡可 `speechSynthesis` 朗读（WebView2 内置）。

### 2.4 通关与结算
- 所有配对完成 → 结算页：得分、用时、步数、错误次数、最高连击、星级。
- "再来一局"（同组重新洗牌翻成背面）/"换一组"（重新抽词）。

---

## 3. 数据接入方案

### 3.1 复用现有服务（关键：几乎零新增后端）
- **取词**：直接复用 `WordMatchGameService.BuildItems(context, maxCount)`，其返回 `List<WordMatchItemDto>`（含 `Id / Word / Meaning / Phonetic / Example`），结构与消消乐一致，前端无需改造。
- **成绩回写**：复用 `WordMatchGameService.ApplyResults(userId, context, results)`，更新 `LearningItem.Status` 与 `ReviewCount`、答错写入错题本。
- **宿主窗体数据桥接**：参考 `WordMatchGameForm`，用 `{type:"init", data, theme}` 经 `PostWebMessageAsJson` 注入，`{type:"gameEnd", results,...}` 经 `WebMessageReceived` 上报。

> 结论：**后端仅需新增一个宿主窗体与入口，无需新增服务类**。若后续要支持"新词/复习词筛选"，再在 `WordMatchGameService.BuildItems` 增加可选参数（向后兼容）。

**错题优先配牌（强化复习闭环）**：给 `WordMatchGameService.BuildItems` 增加可选参数（如 `selection`：`Random` / `WrongFirst` / `ReviewDue`），默认 `Random` 向后兼容。`WrongFirst` 时优先取错题本/`ReviewCount` 低掌握度词条，答对后降权或移出错题，答错再加重。此项与消消乐共用即可，翻牌首版优先接入。

**直接规避消消乐踩过的三个坑（翻牌必须提前做对）**：
1. **配对用局内唯一 key（词条下标），不依赖词库 `Id`**——消消乐因 `Id` 为空曾"全算对"；`Id` 仅用于成绩回写。
2. **`init` 数据在 `NavigationCompleted` 后补发**——沿用消消乐的 `_pendingPayload` 补发逻辑，避免竞态丢消息。
3. **统一 `JsonNamingPolicy.CamelCase`**——避免前后端字段大小写不一致导致前端拿不到数据。

### 3.2 成绩回写语义（记忆翻牌特化）
消消乐里"答对/答错"等于"配对成功/失败"，语义直接。记忆翻牌需明确：
- **每个词条记为一条结果**；
- 该词条对应的两张卡片**在正确配对前，从未被错误翻开配对** → `Correct = true`；
- 若该词条曾卷入错误配对（翻开两张不同 key）→ `Correct = false`；
- 前端在配对成功时上报该词条 id 与其 `correct` 标记；错误配对只用于屏内提示，不单独上报。

这样"翻牌靠记忆命中"会被计入复习闭环，答错词条进错题本。

### 3.3 宿主窗体
新增 `MemoryMatchGameForm`（仿 `WordMatchGameForm`，结构基本一致）：
- 顶部：科目 / 子类别 下拉 + "开始游戏"按钮；
- 主体：`WebView2` 加载 `Resources/MemoryMatchGame/index.html`；
- 复用消消乐窗体中 `InitializeWebViewAsync`、`StartGame`、`WebMessageReceived`、`NavigationCompleted` 补发 pending payload 的写法。

> **必做优化：抽"WebView2 游戏窗体基类"**。现在已有 `WordMatchGameForm`，翻牌是第二个，再复制粘贴就失控。抽基类 `WebView2GameFormBase`，把 WebView2 初始化、`NavigationCompleted` 补发、`init` 数据注入、`gameEnd` 成绩上报、空数据处理、主题联动下沉，消消乐与记忆翻牌共同继承。若求快，可先在翻牌落地后再重构消消乐接入基类。

### 3.4 前端公共件
- 发音、粒子特效、明暗主题、多彩配色、`{type,payload}` 消息协议等可抽共享 `js/css`；若保持独立，至少保证**消息接口一致**，便于未来统一维护。

---

## 4. 前端页面设计（静态 HTML）

### 4.1 文件组织
```
Resources/MemoryMatchGame/
  index.html     # 页面骨架 + 布局
  style.css      # 卡片、翻转、盘面、结算、明暗主题
  game.js        # 配牌、翻牌、配对、计分、结算、通信
```
- 纯原生 JS，无外部依赖/CDN，离线可用。
- 同样支持 `?mock=1` 内置示例数据调试。

### 4.2 页面布局
- 顶部：标题、得分、步数、错误次数、计时（可选）、"换一组/重新开始"。
- 中部：卡片盘面（`grid`，如 4 列自适应；卡片分"背面 / 正面"两种状态）。
- 底部：提示条（"翻开两张卡片，找出单词与其释义的配对"）。

### 4.3 卡片与翻转
- 卡片结构：外层 `.memory-card`，内层 `.inner`（`transform-style: preserve-3d`），包含 `.face-back`（背面，统一图案）与 `.face-front`（正面，单词 或 释义）。
- 翻转：`.memory-card.flipped` → `.inner { transform: rotateY(180deg) }`，CSS transition 实现 3D 翻转。
- 状态类：`flipped`（翻开）、`matched`（已配对）、`wrong`（配对失败抖动）。
- 配色：沿用消消乐的**多彩渐变**方案（单词冷色系 / 释义暖色系），背面用统一中性色 + 图案。
- 明暗主题：与消消乐一致，`body[data-theme]` 控制。

### 4.4 消除特效
- 配对成功：卡片 `matched` 高亮 → 缩放/粒子 → 置灰置为不可点（或缩小淡出）。
- 复用消消乐前端粒子/过渡思路（CSS transform，避免频繁重排）。

---

## 5. WebView2 展示方案

| 项 | 方案 |
|---|---|
| 宿主 | 新增 `MemoryMatchGameForm`（仿 `WordMatchGameForm`） |
| 加载方式 | `Navigate("file:///<Resources>/MemoryMatchGame/index.html")` |
| 数据注入 | `PostWebMessageAsJson({type:"init", data, theme})` |
| 通信 | `WebMessageReceived` 接收 `{type:"gameEnd", results, ...}` |
| 运行时 | `CoreWebView2Environment.CreateAsync(null, cacheDir)`（与现有一致） |
| 错误处理 | WebView2 Runtime 缺失提示；页面文件缺失提示 |

---

## 6. 模块划分与文件清单

| 新增/修改 | 文件 | 说明 |
|---|---|---|
| 新增 | `Forms/MemoryMatchGameForm.cs`（+`.resx`） | 记忆翻牌宿主窗体，继承基类 |
| 新增 | `Forms/WebView2GameFormBase.cs`（推荐） | 公共 WebView2 游戏窗体基类（初始化/注入/上报/主题） |
| 新增 | `Resources/MemoryMatchGame/index.html` | 游戏静态页面 |
| 新增 | `Resources/MemoryMatchGame/style.css` | 游戏样式（含 3D 翻转、多彩配色） |
| 新增 | `Resources/MemoryMatchGame/game.js` | 游戏逻辑 |
| （可选） | `Resources/Shared/`（前端公共 js/css） | 发音、粒子、主题、配色、消息协议共享件 |
| 修改 | `LearningAssistant.csproj` | 复制 `Resources/MemoryMatchGame/*`（及共享件）到输出目录 |
| 修改 | 入口（`MainForm` / `DashboardView` / `WindowManager`） | 新增"🧠 记忆翻牌"功能入口 |
| （可选） | `Services/Learning/WordMatchGameService.cs` | 若加"错题优先/筛选"，扩展 `BuildItems` 参数（默认 `Random` 向后兼容） |
| （可选） | 重构 `WordMatchGameForm` | 接入 `WebView2GameFormBase` 基类 |

> 后端服务类**无需新增**，充分复用 `WordMatchGameService`。

---

## 7. 与"单词消消乐"的差异与复用点

| 维度 | 单词消消乐（已实现） | 记忆翻牌（本方案） |
|---|---|---|
| 卡片初始状态 | 全部正面朝上 | **全部背面朝上** |
| 核心能力 | 找配对（识别） | **记忆配对（回忆）** |
| 配对方式 | 逐张点选、即时反馈 | 翻两张、失败翻回 |
| 数据服务 | `WordMatchGameService` | **复用同一服务** |
| 前端 DTO | `{id,word,meaning,phonetic,example}` | 相同 |
| 成绩回写 | 直接 | 语义特化（见 3.2） |
| 宿主窗体 | `WordMatchGameForm` | 新增 `MemoryMatchGameForm` |

---

## 8. 技术要点与风险

1. **3D 翻转**：需 `perspective` + `transform-style: preserve-3d`，注意旧 WebView2 内核兼容性；降级为简单淡入淡出。
2. **动画 fill 覆盖（消消乐踩过）**：消消乐因 `cardIn ... both` 的 fill 覆盖了消除样式，导致"消除不消失"。翻牌的翻转/配对动画要注意 animation fill 是否会覆盖 `matched` 的置灰/移位状态，必要时用 `animation: none !important` 取消填充。
3. **配对状态机**：一次只能翻两张，翻牌期间锁定其他卡片点击，避免连翻；同类连翻快速退回（见 2.3）。
4. **成绩回写正确性**：按词条 **id 去重**上报，避免同一对重复计入；`correct` 语义按 3.2 定义；配对判断用**局内 key**，`id` 仅回写用。
5. **中文 JSON 编码**：UTF-8 传输，避免释义乱码。
6. **词库不足**：少于所需对数的词条时，前端/C# 双重提示，并可降低 N 继续。
7. **发音可用性**：无语音设备时 `speechSynthesis` 静默降级。
8. **主题一致**：`data-theme` 与 WinForms 联动。
9. **前端调试**：`?mock=1` 内置示例数据，浏览器直接开发。
10. **音效与触屏**：配对/翻牌/发音音效（可静音切换），兼容触屏点击。

---

## 9. 实施步骤（仅计划，不实现）

1. **搭建静态游戏页**：`MemoryMatchGame/index.html + style.css + game.js`，内置示例数据，浏览器可玩（含 3D 翻转、配对状态机、计分、结算）。
2. **宿主窗体**：新增 `MemoryMatchGameForm`，仿 `WordMatchGameForm` 初始化 WebView2、加载页面。
3. **打通数据**：`BuildItems` 取词 → `init` 注入；`gameEnd` 上报 → `ApplyResults` 回写。
4. **接入入口**：`MainForm`/`DashboardView`/`WindowManager` 加入口，选择科目子类别后启动。
5. **打磨**：翻转动画、配对特效、发音、明暗主题、空数据处理、`?mock=1` 调试。
6.（可选）**筛选与难度**：`BuildItems` 支持"新词/复习词/错题"筛选与数量参数；前端难度开关。

---

## 10. 已定决策（推荐默认值）

| # | 决策点 | 定案 |
|---|---|---|
| 1 | 玩法 | **记忆翻牌（Memory Match）**：卡片背面朝上，翻两张配对，失败翻回 |
| 2 | 数据服务 | **完全复用 `WordMatchGameService`**，不新增后端服务 |
| 3 | 宿主 | 新增 `MemoryMatchGameForm`，仿现有消消乐窗体 |
| 4 | 成绩回写 | **是**：按词条 id 去重上报，`correct` 按 3.2 语义，答错进错题本 |
| 5 | 每局对数 | 默认 **N=8**（16 张卡），可调 |
| 6 | 前端技术 | 纯静态 HTML/CSS/JS，无框架，支持 `?mock=1` 调试 |
| 7 | 窗体基类 | **必做：抽 `WebView2GameFormBase`**，消消乐 + 翻牌共同继承 |
| 8 | 配牌策略 | **错题优先**：`BuildItems` 增加 `selection` 参数（默认 `Random` 向后兼容） |
| 9 | 规避消消乐踩坑 | 局内 key 配对 + `CamelCase` 序列化 + `NavigationCompleted` 补发 |
| 10 | 前端公共件 | 发音/粒子/主题/配色/消息协议优先抽共享，至少保持接口一致 |